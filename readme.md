<pre>Credentials file
credentials.lua

-- Credentials
credentials = {
    ["SSID"] = {
        SSID     = "SSID",
        PASSWORD = "SSID password",
        MQTTHOST = 'ip address',
        MQTTPORT = 1883,
        MQTTUSER = 'admin',
        MQTTPASS = 'admin'
    },
}
</pre>

